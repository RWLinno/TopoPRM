# Tests

Run all tests:
```bash
pytest tests/ -v
```

Key test files:
- `test_rewards.py` — Unit tests for all reward functions
- `test_build_dag.py` — DAG extraction tests
- `test_graph.py` — Graph utility tests
