% \begin{abstract}
% Reasoning traces in modern LLMs are rarely purely sequential: they branch into subgoals, reuse intermediate claims, and defer conclusions across steps. Outcome-only supervision ignores this latent structure, producing weak credit assignment on unsupported conclusions, spurious linearised chains, and redundant branches in long reasoning trajectories.
% We propose \method, a topology-aware post-training framework that recovers an implicit dependency DAG from each trace and reuses one deterministic diagnostic pipeline for data construction, reward design, and self-distillation. \method combines correctness-first hierarchical reward aggregation with topology and continuity diagnostics; to keep auxiliary rewards from dominating the correctness signal, we follow prior work and estimate advantages via stratified clipping. The same diagnostics drive \framework (topology-guided self-distillation), where the topology reward acts as a dense token-level signal that compresses long traces while preserving dependency structure.
% % % We propose \method, a topology-aware post-training framework whose dependency DAG is built once offline through a three-phase pipeline (step segmentation, LLM-assisted implicit feature extraction, deterministic DAG construction) and then cached, so that GRPO and evaluation index pre-extracted DAGs without paying any online LLM cost. From each cached DAG, a deterministic scorer emits two structural signals $q_{\mathrm{dag}}$ and $q_{\mathrm{cont}}$, which are gated into a topology-sensitive process reward $R_{\mathrm{topo}}$. \method then aggregates three reward sources $\{R_{\mathrm{out}}, R_{\mathrm{fmt}}, R_{\mathrm{topo}}\}$ in a correctness-first form, and follows prior work to stabilise advantage estimation via stratified clipping. The same gated reward drives \framework (topology-guided self-distillation), where $R_{\mathrm{topo}}$ acts as a dense token-level signal that compresses long traces while preserving dependency structure.
% On public benchmarks, \method reaches 84.5\% accuracy on GSM8K with a 7B model under a unified evaluation protocol, and on in-domain validation data it improves accuracy by $+13.4$ points over outcome-only GRPO while reducing response length by 11\%. The framework uses no human step labels, no learned PRM verifier, and no special graph-generation format.
% \end{abstract}

\begin{abstract}
% 大语言模型中的长思维链推理既非非错即对，也非纯粹线形推导。仅以结果为奖励会忽略不合理的中间步骤，而现有过程奖励模型依赖逐步标注，无法捕捉推理链内部的隐式条件依赖。我们提出 TopoPRM，一种拓扑感知的隐式过程奖励模型：它从每条推理轨迹中提取依赖有向无环图，将条件依赖与顺序连贯融入多源奖励信号。在 TopoPRM 之上，我们设计后训练框架 TGSD，在有监督热启动、以正确性优先的层次化奖励驱动的 GRPO 优化以及拓扑引导的同策略蒸馏中加以应用。在多个公开基准上，本文的多阶段后训练流水线在 7B 与 9B 规模上均超越标准 GRPO，同时将平均响应长度压缩 11.2%。
Long chain-of-thought reasoning in LLMs is neither strictly correct-or-wrong nor purely linear. Outcome-only rewards overlook unjustified intermediate steps, while existing process reward models depend on step-level annotations and fail to capture the implicit conditional dependencies within a reasoning chain.
We propose \method, a \underline{topo}logy-aware implicit \underline{p}rocess \underline{r}eward \underline{m}odel that extracts a dependency DAG from each reasoning trace and integrates conditional dependency and sequential continuity as complementary components of a multi-source reward signal. Building on \method, we design \framework, a post-training framework that combines supervised warm-start, GRPO optimization under a correctness-first hierarchical reward, and topology-guided on-policy distillation.
Across multiple public benchmarks, our method improves average accuracy over standard GRPO at both 7B and 9B scales while generating up to 24\% fewer tokens on GSM8K and about 13\% fewer on the four-primary-benchmark mean, with consistent gains on elementary and general-domain reasoning.
\end{abstract}